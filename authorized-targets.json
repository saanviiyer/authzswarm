{
  "_comment": "AUTHORIZED-USE ONLY. Add ONLY hosts you own or are explicitly authorized to test. Entries match hostname or hostname:port. Prefix a private DNS hostname with private: only when its non-public network is explicitly in scope.",
  "allowedTargets": [
    "localhost",
    "127.0.0.1",
    "[::1]",
    "localhost:3000",
    "127.0.0.1:3000",
    "private:host.docker.internal:3000"
  ]
}
